const CaseStudiesTitle = ({ title }: { title: string }) => (
    <div className='flex w-full items-center'>
      <h1 className='w-full max-w-[800px] text-[32px] lg:text-[42px] font-semibold'>{title}</h1>
    </div>
  );
  
  export default CaseStudiesTitle;